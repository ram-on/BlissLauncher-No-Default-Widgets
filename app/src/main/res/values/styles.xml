<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <style name="HomeScreenTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="workspaceStatusBarScrim">@drawable/workspace_bg</item>
    </style>

    <style name="label_shadow">
        <item name="android:shadowColor">@color/text_shadow_opaque</item>
        <item name="android:shadowRadius">2.0</item>
    </style>

    <style name="WidgetPickerDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">false</item>
        <item name="android:listDivider">@drawable/list_divider_material</item>
    </style>

    <style name="AlertDialogCustom" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">#000000</item>
        <item name="android:typeface">normal</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="BaseIcon" parent="@android:style/TextAppearance.DeviceDefault">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:lines">1</item>
        <item name="android:maxLines">2</item>
        <item name="android:textColor">?android:attr/textColorSecondary</item>
    </style>

    <!-- Theme for the popup container -->
    <style name="PopupItem">
        <item name="android:colorControlHighlight">#E0E0E0</item>
    </style>

    <style name="OverviewTheme">
        <item name="android:colorEdgeEffect">@color/workspaceTextColor</item>
        <item name="android:textColorPrimary">@color/workspaceTextColor</item>
        <item name="android:textColorSecondary">@color/workspaceTextColor</item>
    </style>

    <style name="HomeScreenElementTheme">
        <item name="android:colorEdgeEffect">@color/workspaceTextColor</item>
        <item name="android:textColorPrimary">@color/workspaceTextColor</item>
        <item name="android:textColorSecondary">@color/workspaceTextColor</item>
    </style>
</resources>
