package foundation.e.blisslauncher.domain.entity

/**
 * Application-wide critical business rule which is high level, changes very infrequently.
 * It defines the data representation and the logic on how to manipulate this data
 * and exposes both to the interactors.
 *
 * It should not load the data.
 */
interface Entity